name: realtime_client
description: Shared real-time WebSocket client for alakh_service apps.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  web_socket_channel: ^3.0.3
  equatable: ^2.0.5

dev_dependencies:
  test: ^1.24.0
  mocktail: ^1.0.0
  lints: ^3.0.0
