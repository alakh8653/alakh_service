name: shared_models
description: Shared data models and entities used across all apps in alakh_service.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  equatable: ^2.0.5

dev_dependencies:
  test: ^1.24.0
  lints: ^6.1.0
