name: shared_utils
description: Shared utility functions used across all apps in alakh_service.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  intl: ^0.19.0

dev_dependencies:
  test: ^1.24.0
  lints: ^6.1.0
